# Bootstrap (composition root)

The single module that may import both ports and adapters; it builds wired
services from a validated [`AppConfig`][taxonomaid.config.AppConfig].

::: taxonomaid.bootstrap
